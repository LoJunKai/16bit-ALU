module compare16 (
  input alufn2,
  input alufn1,
  input z,
  input v,
  input n,
  output cmp0
) {
 
  always {
  case (c{alufn2,alufn1}) {
      b01: cmp0 = z;
      b10: cmp0 = (n^v); 
      b11: cmp0 = z|(n^v);
      default: cmp0 = 0;
  }
}
